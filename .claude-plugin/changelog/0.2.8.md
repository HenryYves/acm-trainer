# 0.2.8 修正详情（2026-05-12）

## 触发背景

`/acm-trainer:acm-config` 的 Step 2 修改配置选择页面分 3 次独立 `AskUserQuestion` 调用（Batch 1/2/3），用户每次提交后等 Agent 处理才弹下一页，体验差。`AskUserQuestion` 支持单次调用传多个 question，用户用 `←` `→` 翻页后一次提交。

## 修改内容

### acm-config/SKILL.md

1. **Step 2**：3 次独立 `AskUserQuestion` 调用 → 1 次调用内含 3 个 questions，用户 `←` `→` 翻页一次提交。
2. **Step 2**：新增"以上都没有"处理规则（选了它就忽略其他选中项）。
3. **Step 2**：`has_template` 条件隐藏"变值常量"逻辑保留，移入 Question 3。

### plugin.json

4. 版本 `0.2.7` → `0.2.8`。

## 设计决策

- **不改其他文件**：acm-setup 无类似分页场景，acm 主技能无此问题，仅 acm-config 受影响。
- **不改 MODIFICATION.md**：配置项清单、更新历史条目待下次合并更新。
